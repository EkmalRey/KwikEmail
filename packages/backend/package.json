{
  "name": "kwikemail-backend",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "test": "rm -rf .test-dist && esbuild test/*.test.ts --bundle --platform=node --format=esm --outdir=.test-dist && node --test .test-dist/*.test.js",
    "dev": "wrangler dev",
    "db:migrate:local": "wrangler d1 migrations apply kwikemail --local",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "postal-mime": "^2.3.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.110.0"
  }
}
